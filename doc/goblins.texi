\input texinfo    @c -*- texinfo -*-
@c %**start of header
@setfilename goblins.info
@settitle Spritely Goblins
@documentencoding UTF-8
@documentlanguage en
@c %**end of header

@finalout
@titlepage
@title Spritely Goblins
@author Christine Lemmer-Webber
@end titlepage

@contents

@ifnottex
@node Top
@top Spritely Goblins

This is the manual for @uref{https://spritelyproject.org/#goblins, Spritely Goblins}.  It is released under the
@uref{https://www.apache.org/licenses/LICENSE-2.0, Apache v2 license}, just like Goblins itself.
@end ifnottex

@menu
* What is Goblins?::
* Getting started::
* A high level view of Goblins::
* Tutorial::
* API::
* actor-lib A standard library of sorts::
* Thanks::

@detailmenu
--- The Detailed Node Listing ---

Getting started

* Installing Goblins::
* Setup for hacking on Goblins::
* TEMPORARY Live hacking "fix"::

A high level view of Goblins

* The "vat model" of computation::

Tutorial

* A simple greeter::
* Even easier hacking at the REPL::
* State as updating behavior::
* Methods aren't essential, but they are useful: Methods aren't essential but they are useful. 
* Objects which contain objects::
* Asynchronous message passing::
* Transactions make errors survivable::
* Promise pipelining::
* OCapN, my CapN@!: OCapN my CapN@!. 

API

* Objects::
* Vats::
* Actormaps::
* OCapN::
* Persistence::

Objects

* Spawning objects::
* Synchronous invocation::
* Asynchronous invocation::

Vats

* Starting and stopping vats::

actor-lib: A standard library of sorts

* Cells::
* Methods::

@end detailmenu
@end menu

@node What is Goblins?
@chapter What is Goblins?

@uref{https://spritelyproject.org/#goblins, Spritely Goblins} is a distributed object programming environment.
Features:

@itemize
@item
@strong{Quasi-functional object system:}
Users hold on to references of objects/actors, but objects/actors
are just procedures.
Rather than directly mutating, objects/actors can specify that
they should "become" a new version of themselves when they
handle the next invocation.

@item
@strong{Fully distributed, networked, secure p2p communication:}
Via OCapN, the Object Capability Network (which features CapTP, the
Capability Transport Protocol), you can communicate with other
objects over a network connection.  Distributed object interactions
are safe because of ocap security guarantees.

@item
@strong{Transactional updates:}
Changes happen within a transaction.
If an unhandled exception occurs, we can "roll back" history
as if the message never happened, avoiding confused state
changes throughout the system.

@item
@strong{Time travel:}
We can snapshot old revisions of the system and
interact with them.

@item
@strong{Asynchronous programming with sophisticated promise chaining:}
Asynchronous message passing with promises is supported.
Promises work such that there's no need to wait for promises
to resolve before interacting@dots{} you can communicate with the
future that doesn't even yet exist!
Sent a message to that remote car factory asking for a new car?
Why wait for the car to be delivered@dots{} you can send it a
drive message at the same time, and both messages can be
delivered across the network in a single hop.

@item
@strong{Communicating event loops:}
Objects across multiple event loops (aka "vats") can
communicate, whether in the same OS process or (soon) across
the network.

@item
@strong{Synchronous and asynchronous behavior, integrated but distinguished:}
Both synchronous and asynchronous programming is supported,
but only objects in the same "vat" (event loop) can perform
synchronous immediate calls.
All objects can perform asynchronous calls against each other.

@item
@strong{A library, not a language:}
Goblins is itself a library that can be utilized across a wide
variety of Guile programs.

@item
@strong{Object capability security:}
Goblins itself is built for object capability (ocap) security,
which is to say that you can only operate on the references
you have access to.
Goblins embraces and builds upon this foundation.
(However, full ocap security will require utilizing the
sandboxing features of Guile; demonstrations of this coming soon.)
@end itemize


Goblins' design is inspired by the @uref{http://www.erights.org/, E programming language} and by the
observation that @uref{http://mumble.net/~jar/pubs/secureos/secureos.html, lambda is already the ultimate security mechanism}
(ie, normal argument-passing in programs, if taken seriously/purely,
is already all the security system we need).

Spritely is a wider project to advance decentralized networking
infrastructure, particularly as it pertains to social networks.
Goblins is the heart of the system, on which other layers are built.
At the time of writing, two key versions of Goblins are supported, the
@uref{https://docs.racket-lang.org/goblins/index.html, Racket version of Goblins}, and the @uref{https://www.gnu.org/software/guile/, Guile} version.
This manual is for the Guile edition of Spritely Goblins.

This manual is also very, very incomplete in this early release.
Please see the @uref{https://docs.racket-lang.org/goblins/, manual for the Racket version of Goblins} manual,
most of which applies to the Guile version too.

@node Getting started
@chapter Getting started

@menu
* Installing Goblins::
* Setup for hacking on Goblins::
* TEMPORARY Live hacking "fix"::
@end menu

@node Installing Goblins
@section Installing Goblins

You'll need to install @samp{guile-goblins}.  Either use @uref{https://guix.gnu.org/, Guix} or use your
standard GNU/Linux distro's package for @samp{guile-goblins} (if it exists)
or even better yet run Guix as your standard distro.

See the @samp{HACKING} and @samp{README} files with git checkouts / release
tarballs otherwise to install.

@node Setup for hacking on Goblins
@section Setup for hacking on Goblins

You're going to want/need:

@itemize
@item
@uref{https://www.gnu.org/software/emacs/, GNU Emacs}
@item
@uref{https://www.nongnu.org/geiser/, Geiser}, for hacking on Guile
@end itemize

Then, at minimum, you'll want to put in your .emacs:

@lisp
(show-paren-mode 1)  ; Match parentheses
@end lisp

However you probably also want some combination of:

@itemize
@item
@uref{https://github.com/Fuco1/smartparens, smartparens} (or @uref{https://www.emacswiki.org/emacs/ParEdit, paredit}, which is much more strict@dots{} use
smartparens if uncertain)
@item
@uref{https://github.com/Fanael/rainbow-delimiters, rainbow-delimeters}
@item
@uref{https://www.emacswiki.org/emacs/HighlightParentheses, highlight-parentheses}
@end itemize

Now you'll have a nice hacking setup!  Be sure to read the Geiser
manual.

@node TEMPORARY Live hacking "fix"
@section TEMPORARY: Live hacking "fix"

The traditional way to hack Guile is to use Geiser, both evaluating
from some @samp{foo.scm} file/buffer with more "permanent" code via, for
example, the @samp{C-x C-e} / @samp{C-x C-b} / @samp{C-x C-k} commands to evaluate an
expression, evaluate a whole buffer, or compile a buffer's content,
and switching back and forth between that and the (usually called
@samp{*Geiser Guile REPL*}) experimental-hacking-and-debugging REPL@.

This works with Goblins, but Geiser does some things to temporarily
redirect output/error ports when doing the file/buffer oriented
evaluation, and unfortunately this results in Goblins vats potentially
breaking due to spawning a separate thread which inherit these
temporarily redirected ports, which then are closed when the
evaluation succeeds.  We have a long-term plan to deal with this, but
in the meanwhile, whenever you start a Guile / Geiser hacking session,
first execute:

@example
> (use-modules (goblins))
@end example

@dots{} from the @samp{*Geiser Guile REPL*}.  Otherwise vats may mysteriously
"hang".  We know this is an annoyance!  It'll be fixed soon!

@node A high level view of Goblins
@chapter A high level view of Goblins

Let's start with a sketch of a network of Goblins objects wired
to communicate with each other:

@example
.----------------------------------.         .----------------------.
|            Machine 1             |         |       Machine 2      |
|            =========             |         |       =========      |
|                                  |         |                      |
| .--------------.  .---------.   .-.       .-.                     |
| |    Vat A     |  |  Vat B  |   |  \______|  \_   .------------.  |
| |  .---.       |  |   .-.   | .-|  /      |  / |  |    Vat C   |  |
| | (Alice)----------->(Bob)----' '-'       '-'  |  |  .---.     |  |
| |  '---'       |  |   '-'   |    |         |   '--->(Carol)    |  |
| |      \       |  '----^----'    |         |      |  '---'     |  |
| |       V      |       |         |         |      |            |  |
| |      .----.  |       |        .-.       .-.     |  .------.  |  |
| |     (Alfred) |       '-------/  |______/  |____---( Carlos ) |  |
| |      '----'  |               \  |      \  |     |  '------'  |  |
| |              |                '-'       '-'     '------------'  |
| '--------------'                 |         |                      |
|                                  |         |                      |
'----------------------------------'         '----------------------'
@end example

In the above diagram, we see:

@itemize
@item
Two machines (@samp{Machine 1} and @samp{Machine 2}, running separately from
each other, but connected to each other over the network via OCapN
and CapTP@.

@item
@samp{Vat A} and @samp{Vat B} are event loops which live on @samp{Machine 1},
and @samp{Vat C} is an event loop which live son @samp{Machine 2}.

@item
The individual objects (represented by circles) live in @emph{vats},
aka event loops which contain objects.  @samp{Alice} and
@samp{Alfred} live in @samp{Vat A}, @samp{Bob} lives in @samp{Vat B}, and @samp{Carol} and
@samp{Carlos} live on @samp{Vat C}.  (While we've given these objects
human-like names, they're just Goblins objects.)

@item
The arrows between the objects represent references these objects
have to each other.  @samp{Alice} has references to both @samp{Alfred} and
@samp{Bob}.  @samp{Bob} has a reference to @samp{Carol}.  @samp{Carlos} has a reference
to @samp{Bob}.

@item
Two objects which are in the same vat are considered @emph{near} each
other, and thus can invoke each other synchronously, whereas any
objects not in the same vat are considered @emph{far} from each other.
Any objects can invoke each other by asynchronous message
passing@dots{} assuming they have a reference to each other.

@item
Not pictured: each vat has an @emph{actormap}, an underlying
transactional heap used for object communication.  This is what
permits transactionality and time travel.  (Actormaps can also used
independently of vats for certain categories of applications.)
@end itemize

Another way to think about this is via the following abstraction
nesting dolls:

@example
(machine (vat (actormap @{refr: object-behavior@})))
@end example

@itemize
@item
@strong{Machines}, which are computers on the network, or more
realistically, operating system processes, which contain@dots{}
@item
@strong{Vats}, which are communicating event loops, which contain@dots{}
@item
@strong{Actormaps}, transactional heaps, which contain@dots{}
@item
A mapping of @strong{References} to @strong{Object Behavior}.
@end itemize

@menu
* The "vat model" of computation::
@end menu

@node The "vat model" of computation
@section The "vat model" of computation

As said, Goblins follows what is called the @emph{vat model} of
computation.
A @emph{vat} is simply an event loop that manages a set of
objects which are @emph{near} to each other (and similarly, objects outside
of a vat are @emph{far} from each other).

Objects which are @emph{near} can perform synchronous call-return
invocations in a manner familiar to most sequential programming
languages used by most programmers today.  Aside from being
a somewhat more convenient way to program, sequential invocation
is desirable because of cheap @emph{transactionality}, which we shall
expand on more later.  In Goblins, we use the @samp{$} operator
to perform synchronous operations.

Both @emph{near} and @emph{far} objects are able to invoke each other
asynchronously using asynchronous message passing (in the same style
as the @emph{classic actor model}).  It does not
generally matter whether or not a @emph{far} object is running within the
same OS process or machine or one somewhere else on the
network for most programming tasks; asynchronous
message passing works the same either way.  In Goblins, we use the
@samp{<-} operator to perform asynchronous operations.

For both programmer convenience and for networked efficiency, Goblins
supports @emph{promise pipelining}: messages can be sent to promises which
have not yet resolved, and will be forwarded to the target once the
promise resolves.
The sender of the message is handed back a promise to which it can
supply callbacks, listening for the promise to be fulfilled with a
value or broken (usually in case of an unexpected error).

As usual in the vat model of computation, individual message sends to
a vat (event loop) are queued and then handled one @emph{turn} at a time,
akin to the way board game players take turns around a table (which is
indeed the basis for the term @emph{turn}).

The message, addressing a specific object, is passed to the recipient
object's current behavior.  This object may then invoke other
@emph{near} objects (residing within the same vat), which may themselves
invoke other near objects in a synchronous and sequential call-return
style familiar to most users of most contemporary programming
languages.  Any of these invoked objects may also change their
state/behavior (behavior changes appear purely functional in Goblins;
invocations of other actors do not), spawn new objects, invoke
ordinary expressions from the host language, or send asynchronous
messages to other objects (which are only sent if the @emph{turn} completes
successfully).

While the @emph{vat model} of computation is not new (it originates in the
@uref{http://erights.org/, E} programming language and can trace some of its ideas back to E's
predecessor @uref{http://erights.org/history/joule/, Joule}, and has since reappeared in systems such as
@uref{https://agoric.com/, Agoric's} @uref{https://github.com/Agoric/agoric-sdk/tree/master/packages/SwingSet, SwingSet} kernel), Goblins brings some novel contributions to
the table in terms of transactionality and time-travel debugging,
enhancing an already powerful distributed programming paradigm.

@node Tutorial
@chapter Tutorial

@menu
* A simple greeter::
* Even easier hacking at the REPL::
* State as updating behavior::
* Methods aren't essential, but they are useful: Methods aren't essential but they are useful. 
* Objects which contain objects::
* Asynchronous message passing::
* Transactions make errors survivable::
* Promise pipelining::
* OCapN, my CapN@!: OCapN my CapN@!. 
@end menu

@node A simple greeter
@section A simple greeter

First, let's import Goblins:

@example
> (use-modules (goblins))
@end example

(@strong{NOTE}: See @ref{TEMPORARY Live hacking "fix"} if you haven't.)

Every Goblins actor / object has to live in a vat somewhere.
So, let's make a vat to play around with:

@example
> (define a-vat
    (spawn-vat))
;; This will make interacting with the vat a bit easier
> (define-vat-run a-run a-vat)
@end example

Objects/actors are instantiated with @samp{spawn} and can be synchronously
invoked with @samp{$} or asynchronously invoked with @samp{<-}.
All of these operations need to operate within a Goblins-aware context,
and so @samp{a-run} "bootstraps" some code within the vat.

Now let us implement a friend who will greet us:

@example
(define (^greeter bcom our-name)   ; constructor (outer procedure)
  (lambda (your-name)              ; behavior    (inner procedure)
    (format #f "Hello ~a, my name is ~a!"
            your-name our-name)))
@end example

Let's give it a try by instantiating an instance of @samp{^greeter} using
@samp{spawn} and assign her to the variable @samp{alice}:

@example
> (define alice
    (a-run (spawn ^greeter "Alice")))
@end example

The first argument to @samp{spawn} is the constructor we will use to make
our Goblins object, in this case @samp{^greeter}.
The remaining arguments will be passed to the constructor,
along with an implicit argument which can be used to change object
behavior called @samp{bcom} (pronounced "become").
Thus @samp{"Alice"} maps to @samp{our-name}.

We can invoke @samp{alice} by using @samp{$} for a synchronous call-return
operation:

@example
> (a-run ($ alice "Alfred"))
=> "Hello Alfred, my name is Alice!"
@end example

The first argument to @samp{$} is the object to be invoked, the remaining
arguments are passed to the "behavior" of the object.
Thus @samp{your-name} in @samp{^greeter}'s behavior procedure is bound to
@samp{"Bob"}.
The behavior procedure thus proceeds with formatting and returning the
greeting string.

@node Even easier hacking at the REPL
@section Even easier hacking at the REPL

Here's a  little aside that  can make  your hacking with Goblins more
enjoyable.

Setting up a vat can be annoying when all you want to do is test
the code you've written.
While @samp{define-vat-run} simplifies use a little bit, it's also a bit
funny.

In "normal" Goblins code we'd just write:

@example
(define alice
  (spawn ^greeter "Alice"))
@end example

But at the REPL we had to insert this @samp{a-run} thing in the middle:

@example
> (define alice
    (a-run (spawn ^greeter "Alice")))
@end example

If you just want to write some quick code for experimenting at the
REPL, we have a comma command which can make your life a bit easier,
@samp{,vrun} (or @samp{,vr} for short):

@example
> (use-modules (goblins vrun))
> ,vrun (define alice (spawn ^greeter "Alice"))
> ,vrun ($ alice "Bob")
=> "Hello Bob, my name is Alice!"
> ,vr ($ alice "Bob")    ; same thing
=> "Hello Bob, my name is Alice!"
@end example

@samp{,vrun} stands for "vat run", because it's a quick way to test.  It
tries to be smart and unwrap @samp{define} statements and such like the
above so that they still can be bound at the REPL@.

@node State as updating behavior
@section State as updating behavior

Let's introduce a simple cell which stores a value.
This cell will have two methods:
@samp{'get} retrieves the current value, and @samp{'set} replaces
the current value with a new value.

@example
(define* (^cell bcom #:optional [val #f])
  (case-lambda
    (()         ; get
     val)
    ((new-val)  ; set
     (bcom (^cell bcom new-val)))))
@end example

@samp{case-lambda} allows for dispatching depending on the number of
arguments, so here we're choosing that if no arguments are provided,
our cell shares the current value, and if one argument is provided,
the cell updates itself to become a cell storing the new value.

Let's try it.
Cells hold values, and so do treasure chests, so let's make
a treasure chest flavored cell.
Taking things out and putting them back in is easy:

@example
> (define chest
    (a-run (spawn ^cell "sword")))
> (a-run ($ chest))
=> "sword"
> (a-run ($ chest "gold"))
> (a-run ($ chest))
=> "gold" 
@end example

Now we can see what @samp{bcom} is: a capability specific to this object
instance which allows it to change its behavior!
(For this reason, @samp{bcom} is pronounced "become"!)

@node Methods aren't essential but they are useful
@section Methods aren't essential, but they are useful

The most common way to select different sub-behaviors isn't by the
number of arguments though.
It's much more common to have "methods" that are selected by name.
We can use the @samp{methods} macro to do just that:

@example
(use-modules (goblins actor-lib methods))

(define* (^mcell bcom #:optional [val #f])
  (methods
   ((get)
    val)
   ((set new-val)
    (bcom (^mcell bcom new-val)))))
@end example

The use of @samp{^mcell} is similar, but now we pass in a first argument
as a symbol selecting the method, and the rest of the arguments are
passed to the object:

@example
> (define jar
    (a-run (spawn ^mcell "cookies")))
> (a-run ($ jar 'get))
=> "cookies"
> (a-run ($ jar 'set "crumbs"))  
> (a-run ($ jar 'get))
=> "crumbs"
@end example

Methods aren't particularly magical@dots{} the @samp{methods} macro just
returns a procedure which is convenient to use for procedure dispatch:

@example
> (define foo-or-bar
    (methods
     ((foo)
      'called-foo)
     ((bar arg)
      (list 'called-bar-with arg))))
> (foo-or-bar 'foo)
=> called-foo
> (foo-or-bar 'bar "meep")
=> (called-bar-with "meep")
@end example

So really, @samp{methods} just makes a particularly useful kind of
behavior procedure for us, one which takes a first argument for
the method name, grabs the rest of the arguments, and then applies
them to the relevant method.

@node Objects which contain objects
@section Objects which contain objects

Objects can also contain and define other object references, including
in their outer constructor procedure.

Here is the definition of a "counting greeter" we call @samp{^cgreeter}:

@example
(define (^cgreeter _bcom our-name)
  (define times-called
    (spawn ^mcell 0))
  (methods
   ((get-times-called)
    ($ times-called 'get))
   ((greet your-name)
    ;; increase the number of times called
    ($ times-called 'set
       (+ 1 ($ times-called 'get)))
    (format #f "[~a] Hello ~a, my name is ~a!"
            ($ times-called 'get)
            your-name our-name))))
@end example

As we can see near the top, @samp{times-called} is instantiated as an
@samp{^cell} like the one we defined earlier.
The current value of this cell is returned by @samp{get-times-called}
and is updated every time the @samp{greet} method is called:

@example
> (define julius
    (a-run (spawn ^cgreeter "Julius")))
> (a-run ($ julius 'get-times-called))
=> 0
> (a-run ($ julius 'greet "Gaius"))
=> "[1] Hello Gaius, my name is Julius!"
> (a-run ($ julius 'greet "Brutus"))
=> "[2] Hello Brutus, my name is Julius!"
> (a-run ($ julius 'get-times-called))
=> 2
@end example

@node Asynchronous message passing
@section Asynchronous message passing

We have shown that the behavior of objects may be invoked
synchronously with @samp{$}.
However, this only works if two objects are both defined on the same
machine on the network and the same event loop within that machine.

To demonstrate this, let's create a new vat, @samp{b-vat} (as well as a
@samp{b-run} to make REPL examples a bit easier to demonstrate):

@example
> (define b-vat
    (spawn-vat))
> (define-vat-run b-run b-vat)
@end example

Now, recall our friend @samp{alice} who we defined on vat A@.
The following works just fine:

@example
> (a-run ($ alice "Alfred"))
=> "Hello Alfred, my name is Alice!"
@end example

But since Alice works on A, we cannot synchronously invoke her with
@samp{$} on vat B:

@example
> (b-run ($ alice "Bob"))
;; === Caught error: ===
;;  message: #<<message> to: #<local-object> resolve-me: #f args: ()>
;;  exception: ... "Not in the same vat: ~a" #<local-object ^greeter>) ...
@end example

This is where @samp{<-} comes in.
In contrast to @samp{$}, @samp{<-} can be used against objects which live
anywhere, even on remote machines.
However, unlike invocation with @samp{$}, we do not get back an
immediate result, we get a promise:

@example
> (b-run (<- alice "Bob"))
=> #<promise>
@end example

This promise must be listened to.
The procedure to listen to promises in Goblins is called
@samp{on}:

@example
> (b-run
   (on (<- alice "Bob")
     (lambda (got-back)
       (format #t "Heard back: ~a\n"
               got-back))))
; Prints out, at some point in the future:
;   "Heard back: [3] Hello Bob, my name is Alice!"
@end example

Not all communication goes as planned, especially in a distributed
system.
@samp{on} also supports the keyword arguments of @samp{#:catch} and @samp{#:finally},
which both accept a procedure defining handling errors in the former case
and code which will run regardless of successful resolution or failure
in the latter case:

@example
> (define (^broken-ben _bcom)
    (lambda ()
      (error "Yikes, I broke!")))
> (define broken-ben
    (b-run (spawn ^broken-ben)))
> (a-run
   (on (<- broken-ben)
       (lambda (what-did-ben-say)
         (format #t "Ben says: ~a\n" what-did-ben-say))
       #:catch
       (lambda (err)
         (format #t "Got an error: ~a\n" err))
       #:finally
       (lambda ()
         (display "Whew, it's over!\n")))
@end example

@node Transactions make errors survivable
@section Transactions make errors survivable

Mistakes happen, and when they do, we'd like damage to be minimal.
But with many moving parts, accomplishing this can be difficult.

However, Goblins makes our life easier.
To see how, let's intentionally insert a couple of print debugging
lines (with @samp{pk}, which is pronounced and means "peek")
and then an error:

@example
(define (^borked-cgreeter _bcom our-name)
  (define times-called
    (spawn ^cell 0))
  (methods
    ((get-times-called)
     ($ times-called 'get))
    ((greet your-name)
     (pk 'before-incr ($ times-called 'get))
     ;; increase the number of times called
     ($ times-called 'set
        (+ 1 ($ times-called 'get)))
     (pk 'after-incr ($ times-called 'get))
     (error "Yikes")
     (format #f "[~a] Hello ~a, my name is ~a!"
             ($ times-called 'get)
             your-name our-name))))
@end example

Now let's spawn this friend and invoke it:

@example
> (define horatio
    (a-run (spawn ^borked-cgreeter "Horatio")))
> (a-run ($ horatio 'get-times-called))
=> 0
> (a-run ($ horatio 'greet "Hamlet"))
; pk debug: (before-incr 0)
; pk debug: (after-incr 1)
; ice-9/boot-9.scm:1685:16: In procedure raise-exception:
;   Yikes
; Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
@end example

Whoops!  Looks like something went wrong!
We can see from the @samp{pk} debugging that the @samp{times-called} cell should
be incremented to 1.
And yet@dots{}

@example
> (a-run ($ horatio 'get-times-called))
=> 0
@end example

We will cover this in greater detail later, but the core idea here
is that synchronous operations run with @samp{$} are all done together
as one transaction.
If an unhandled error occurs, any state changes resulting from
synchronous operations within that transaction will simply not
be committed.
This is useful, because it means most otherwise difficult cleanup
steps are handled automatically.

@node Promise pipelining
@section Promise pipelining

@quotation
"Machines grow faster and memories grow larger.
But the speed of light is constant and New York is not getting any
closer to Tokyo."

--- Mark S@. Miller,
    @uref{http://www.erights.org/talks/thesis/, Robust Composition: Towards a Unified Approach to Access Control and Concurrency Control}

@end quotation

Promise pipelining provides two different features at once:

@itemize
@item
A convenient developer interface for describing a series of
asynchronous actions, allowing for invoking the objects which
promises will point to before they are even resolved (sometimes
before the objects even exist!)
@item
A network abstraction that eliminates many round trips
@end itemize

Consider the following car factory, which makes cars carrying
the company name of the factory:

@example
;; Create a "car factory", which makes cars branded with
;; company-name.
(define (^car-factory bcom company-name)
  ;; The constructor for cars we will create.
  (define (^car bcom model color)
    (methods                      ; methods for the ^car
     ((drive)                    ; drive the car
      (format #t "*Vroom vroom!*  You drive your ~a ~a ~a!"
              color company-name model))))
  ;; methods for the ^car-factory instance
  (methods                        ; methods for the ^car-factory
   ((make-car model color)       ; create a car
    (spawn ^car model color))))
@end example

Here is an instance of this car factory, which we will call
@samp{fork-motors}:

@example
;; Interaction on machine A
> (define fork-motors
    (a-run
     (spawn ^car-factory "Fork")))
@end example

Since asynchronous message passing with @samp{<-} works across machines,
it does not matter whether interacitons with =fork-motors are local
or via objects communicating over the network.
We will treat @samp{fork-motors} as living on machine A, and so the
following interactions will happen with invocations originating from
machine B@.

Let's send a message to @samp{fork-motors} invoking the @samp{'make-car} method,
receiving back a promise for the car which will be made, which we
shall name @samp{car-vow} (@samp{-vow} being the conventional suffix given
for promises in Goblins):

@example
;; Interaction on machine B, communicating with fork-motors on A
> (define car-vow
    (b-run
     (<- fork-motors 'make-car "Explorist" "blue")))
@end example


So we have a @emph{promise} to a future car reference, but not the
reference itself.
We would like to drive the car as soon as it rolls off the lot of the
factory, which of course involves sending a message to the car.

Without promise pipelining, making use of the tools we have already
shown (and following the pattern most other distributed programming
systems use), we would end up with something like:

@example
;; Interaction on machine B, communicating with A
> (b-run
   (on car-vow                    ; B->A: first resolve the car-vow
       (lambda (our-car)          ; A->B: car-vow resolved as our-car
         (on (<- our-car 'drive)  ; B->A: now we can message our-car
             (lambda (val)        ; A->B: result of that message
               (format #f "Heard: ~a\n" val))))))
@end example

With promise pipelining, we can simply message the promise of the car
directly.
The first benefit can be observed from code compactness, in that
we do not need to do an @samp{on} of @samp{car-vow} to later message @samp{our-car},
we can simply message @samp{car-vow} directly:

@example
;; Interaction on machine B, communicating with A
> (b-run
   (on (<- car-vow 'drive)     ; B->A: send message to future car
       (lambda (val)             ; A->B: result of that message
         (format #f "Heard: ~a\n" val))))
@end example

While clearly a considerable programming convenience, the other
advantage of promise pipelining is a reduction of round-trips, whether
between our event-loop @emph{vats} or across machines on the network.

This can be understood by looking at the comments to the right of the
two above code interactions.  The message flow in the first case looks
like:

@example
B => A => B => A => B
@end example


The message flow in the second case looks like:

@example
B => A => B
@end example


In other words, machine B can say to machine A: "Make me a car,
and as soon as that car is ready, I want to drive it!"

With this in mind, the promise behind Mark Miller's quote at the
beginning of this section is clear.@footnote{Like so many examples in this document, the designs
of promise pipelining and the explanation of its value come from the E
programming language, the many contributors to its design, and Mark
S@. Miller's extraordinary work documenting that work and its history.
If you find this section interesting, both the the @uref{http://www.erights.org/elib/distrib/pipeline.html, Promise Pipelining}
page from @uref{http://erights.org, erights.org} and sections 2.5 and 16.2 of
@uref{http://www.erights.org/talks/thesis/markm-thesis.pdf, Mark Miller's dissertation}.}
If two objects are on opposite ends of the planet, round trips
are unavoidably expensive.
Promise pipelining both allows us to make plans as programmers
and allows for Goblins to optimize carrying out those steps as
bulk operations over the network.

@node OCapN my CapN@!
@section OCapN, my CapN@!

TODO

@node API
@chapter API

Here lies the core procedures for building and manipulating Goblins
programs!

Unless where stated, most code from this section can be found by
importing:

@example
(use-modules (goblins))
@end example

@menu
* Objects::
* Vats::
* Actormaps::
* OCapN::
* Persistence::
@end menu

@node Objects
@section Objects

Objects are the core of Goblins.
In general, you'll need to be within an ambient "Goblins Context" (TM)
in order to start programming against them, see @ref{Vats} for most event
loop type programming, or @ref{Actormaps} if you want to play with more low-level
control over transactional operations.
(However, most users will just use vats.)

@menu
* Spawning objects::
* Synchronous invocation::
* Asynchronous invocation::
@end menu

@node Spawning objects
@subsection Spawning objects

Objects can be spawned with the @samp{spawn} operator:

@example
(spawn CONSTRUCTOR ARGS ...)
;=> <local-refr>
@end example

where @samp{CONSTRUCTOR} is a procedure which itself returns a procedure
representing an object's current behavior.
The @samp{CONSTRUCTOR} is implicitly passed a first argument, traditionally
labeled @samp{bcom}, which can be used to change an object's behavior.
The @samp{ARGS} are passed as the remaining arguments to the constructor.

So for example, let's say we have the following constructor:

@example
(define* (^cell bcom #:optional val)  ; constructor (outer procedure)
  (case-lambda                        ; behavior    (inner procedure)
    (()                               ; 0-argument invocation (getter)
     val)                             ;   (return current value)
    ((new-val)                        ; 1-argument invocation (setter)
     (bcom (^cell bcom new-val)))))   ;   ("become" ^cell with new value)
@end example

And so@dots{}

@example
(define some-cell (spawn ^cell 42))
@end example

will bind @samp{val} to @samp{42}.

Let's look at the @samp{bcom} argument in depth.
This is a capability which allows the actor to change its own behavior.
It must be called in a tail position, since @samp{bcom} actually returns
a datastructure demonstrating that this object is choosing to change
its value (part of Goblins' quasi-functional design.)

@samp{bcom} also can take a second argument, which will be returned from
the object's invocation as a return value, allowing for both an object
change its own behavior and return a value at the same time during an
invocation.

The following actor represents a consumable object that returns a
string explaining its current status:

@example
(define (^drink bcom drink-name portions)
  (define (drinkable-beh portions)   ; "-beh" is a common suffix for "-behavior"
    (lambda ()
      (define portions-left
        (- portions 1))
      (if (zero? portions-left)
          (bcom empty-beh
                (format #f "*GLUG!* You drink your ~a. It's empty!"
                        drink-name))
          (bcom (drinkable-beh portions-left)
                (format #f "*GLUG!* You drink your ~a. Still some left!"
                        drink-name)))))
  (define (empty-beh)
    "Sadly, your glass appears to be empty!")
  (drinkable-beh portions))
@end example

Now, playing around at the REPL, we can see how this works:

@example
> ,vr (define root-beer (spawn ^drink "root beer" 3))
> ,vr ($ root-beer)
=> "*GLUG!* You drink your root beer. Still some left!"
> ,vr ($ root-beer)
=> "*GLUG!* You drink your root beer. Still some left!"
> ,vr ($ root-beer)
=> "*GLUG!* You drink your root beer. It's empty!"
> ,vr ($ root-beer)
=> "Sadly, your glass appears to be empty!"
@end example

As you can see above, @samp{bcom} is also a handy way to construct state
machines, as @samp{^drink} moves from drinkable to empty as it is consumed.

@node Synchronous invocation
@subsection Synchronous invocation

To synchronously invoke two objects within the same vat / actormap,
use @samp{$}:

@example
($ near-object args ...)
;=> something
@end example

Provide a synchronous call against the current message handler of
@samp{NEAR-REFR}, which must be a @emph{near} @samp{live-refr?}
in the same vat as the currently running actor context.
The value returned is that which is returned by the
current behavior of @samp{NEAR-REFR} upon invocation (or, if
@samp{NEAR-REFR} chose to @samp{bcom} something else, the second
argument passed to its @samp{bcom}, or is @samp{unspecified?} if not provided.)

@node Asynchronous invocation
@subsection Asynchronous invocation

@example
(<- near-object arg ...)
;=> <local-promise>
@end example

Like @samp{$}, but called asynchronously and returns a @emph{promise},
which may be handled with @samp{on}.
Unlike @samp{$}, @samp{<-} is not limited to only @emph{near} objects;
interaction with @emph{far} actors are permitted as well.

@example
(<-np [actor-refr live-refr?] [arg any/c] ...)
@end example

Like @samp{<-} but does not return (or require the overhead of) a
promise.
@samp{<-np} is effectively an optimization; where promises are not
needed, using @samp{<-np} over @samp{<-} is a good idea.

@example
(on vow
    [fulfilled-handler]
    [#:catch broken-handler]
    [#:finally finally-handler]
    [#:promise? #f])
@end example

Sets up promise handlers for @samp{vow}, which is typically a
@samp{live-refr?} to a @emph{promise} but may be anything.

@samp{fulfilled-handler}, @samp{broken-handler}, and @samp{finally-handler}
all, if specified, may be either a procedure (the most common case)
which is run when @samp{vow} becomes resolved, or a reference to an
actor that should be messaged when the promise is resolved with the
same arguments that would be passed to an equivalent procedure.
As their names suggest, @samp{fulfilled-handler} will run if a promise is
fulfilled and takes one argument, the fulfilled value.
@samp{broken-handler} will run if a promise is broken and takes one
argument, the error value.
@samp{finally-handler} will run when a promise is resolved, no matter the
outcome and is called with no arguments.

If @samp{#:promise?} is set to @samp{#t}, then @samp{on}
will itself return a promise.
The promise will be resolved as follows:

@itemize
@item
If @samp{vow} is fulfilled and @samp{fulfilled-handler} is not
provided, the promise returned will share @samp{vow}'s resolution.
@item
If @samp{vow} is broken and @samp{broken-handler} is not
provided, the promise returned will share @samp{vow}'s broken result.
@item
If @samp{vow} is fulfilled and @samp{fulfilled-handler} is provided,
the resolution will be whatever is returned from @samp{fulfilled-handler},
unless @samp{fulfilled-handler} raises an error, in which case the promise
will be broken with its error-value set to this exception.
@item
If @samp{vow} is broken and @samp{broken-handler} is provided,
the resolution will be whatever is returned from @samp{broken-handler},
unless @samp{broken-handler} raises an error, in which case the promise
will be broken with its error-value set to this exception (which
may even be the original exception).
@end itemize

@node Vats
@section Vats

In order to start programming with Goblins, you will need to boot up a
vat somewhere.

In general, Goblins uses @uref{https://github.com/wingo/fibers/, Fibers} to implement vats.  However, it is
intended that users should be able to roll their own vats should they
have "unusual" event loop needs.  More on how to do this in a future
iteration of these docs.  ;)

@menu
* Starting and stopping vats::
@end menu

@node Starting and stopping vats
@subsection Starting and stopping vats

@example
> (define my-vat (spawn-vat))
> my-vat
=> #<procedure (vat-controller)>
@end example

Running @samp{spawn-vat} returns what's called a "vat controller".
Running with no argument runs some code in the vat, blocking until
a response is returned@dots{} useful for bootstrapping vats, or
hacking at the REPL:

@example
> (define my-vat (spawn-vat))
=> #<procedure (vat-controller)>
> (my-vat
    (lambda ()
      (define alice (spawn ^greeter "Alice"))
      ($ alice "Bob")))
=> "Hello Alice, my name is Bob!"
@end example

You can also send it the @samp{'halt} method to stop the vat,
and @samp{'running?} to check its status:

@example
> (my-vat 'halt)
> (my-vat 'running?)
=> #f
@end example

@strong{NOTE:} In the future, we would like vats to automatically halt when
no more references exist and thus the vat can do no more work.  The
Racket version of vats works this way, it would be good for the Guile
version to do the same.  In the meanwhile, you'll need to use @samp{'halt}
manually.

@node Actormaps
@section Actormaps

@node OCapN
@section OCapN

@node Persistence
@section Persistence

@node actor-lib A standard library of sorts
@chapter actor-lib: A standard library of sorts

@menu
* Cells::
* Methods::
@end menu

@node Cells
@section Cells

@node Methods
@section Methods

@node Thanks
@chapter Thanks

This manual was primarily written by Christine Lemmer-Webber.

Many of the ideas are borrowed from @uref{http://www.erights.org/talks/thesis/markm-thesis.pdf, Mark Miller's dissertation},
which is an excellent, if nerdy, read.

Jessica Tallon, Jonathan A@. Rees, and Randy Farmer provided review and
feedback.

@bye