(define-module (goblins actor-lib let-on)
  #:use-module (goblins actor-lib on)
  #:re-export (let-on let*-on))

(issue-deprecation-warning
   "(goblins actor-lib let-on) has been replaced with (goblins actor-lib on)")
