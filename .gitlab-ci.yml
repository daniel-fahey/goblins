image: registry.gitlab.com/spritely/guile-goblins:latest

stages:
  - test

unit-tests:
  stage: test
  script:
    - sh ./bootstrap.sh
    - ./configure
    - make distcheck
    # Check we can build the HTML docs
    - make html
